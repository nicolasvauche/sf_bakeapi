{% extends 'base.html.twig' %}

{% block title %}Tous les Produits{% endblock %}

{% block body %}
    <h1>Tous les Produits</h1>

    <ul>
        {% for product in products %}
            <li>
                <strong>{{ product.name }}</strong>
                -
                {{ product.price|number_format(2, ',', ' ') }}€
                -
                {{ product.status }}
                chez
                {{ product.user.bakeryName }}
            </li>
        {% endfor %}
    </ul>
{% endblock %}
